<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manohar.drioditor">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".EditCodeActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ml_code"
            android:label="@string/title_activity_ml_code" />
        <activity
            android:name=".dialog_act"
            android:label="@string/title_activity_dialog_act" />
        <activity
            android:name=".ml_template"
            android:label="@string/title_activity_ml_template" />
        <activity
            android:name=".template"
            android:label="@string/title_activity_template" />
        <activity android:name=".SettingsPrefActivity" />
        <activity android:name=".pdf_viewer"></activity>
    </application>

</manifest>